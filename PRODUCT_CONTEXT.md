# Product Context

> 本文档是简辑产品与用户的稳定 context source，供设计、前端与 Agent 任务读取。事实均有 repo、运行证据或用户确认为依据；视觉方案推导见 `## Design Direction`。最后更新：2026-09-22（依据：README、CLAUDE.md、真实运行截图诊断、用户采访三问）。

## Product Purpose

简辑是 Windows / Linux 本地桌面应用（Electron），为电商短视频做**批量包装**：用户导入产品原视频、手动填写展示文字（价格等）、选择规则模板，Agent 为每条素材独立搭配四角贴纸与滤镜，本地 FFmpeg 批量导出独立 MP4。

解决的核心问题：同一批产品素材需要快速产出**多条不雷同的成品视频**用于投放/发布，手工逐条做包装（贴价格、贴促销贴纸、调滤镜）成本高且慢。简辑把"每条视频的包装决策"委托给模型，把"渲染与合规"留在本地。

不属于本产品：剪辑、拼接、转写、配音、素材生成。包装是唯一价值。

## Target Users

- **主要用户：开发者本人及其小团队，每天高频批量出片**（用户确认）。熟练用户，流程已熟，效率与信息密度优先于引导。
- 用户特征：非专业视频编辑，电商运营视角；关心出片数量、速度、稳定性，不关心设计工具式的精细控制。
- 使用环境：桌面（Windows / Linux），窗口化桌面应用，无移动端场景；常见窗口宽度约 1000–1600px，最小约 760px。
- 证据：本机 69 张上传促销贴纸、多个真实产品项目（氨糖膏、舒鼾膏、蝴蝶贴）、69+ 条已完成批次。

## Jobs To Be Done

1. **主 Job**：给我一批产品原视频，快速得到 N 条可以直接发布的、带价格与促销包装的成品视频，且每条看起来不完全一样。
2. 辅助 Job：原视频角落已有贴纸时，不要叠丑——要么避开、要么用我的贴纸盖住。
3. 辅助 Job：出片失败或效果不佳时，能低成本重试，不重复花模型调用。
4. 辅助 Job：管理我的贴纸素材库，供 Agent 选用。

## Primary User Goals

1. 一次设置，批量出片（条数可超素材数，向上取整）。
2. 价格/展示文字准确——这是合规与转化的关键信息，必须用户手动输入、原样呈现。
3. 过程可预期：进度可见、失败明确、重试不重复创作。
4. 原片与数据安全：本地处理、不覆盖源文件、不泄露凭据。

## Critical User Flows

1. **主闭环（每天多次）**：导入素材 → 包装设置（模板/展示文字/条数/输出设置）→ 交给 Agent → 作品列表跟进导出 → 播放验收。
2. 追加制作：从作品列表对已完成批次追加 N 条（随机装饰、零模型调用）。
3. 覆盖贴纸配置：开启覆盖 → 自动识别或手动设置 → 保存覆盖设置。
4. 贴纸库管理：上传、查看、删除。
5. 模型配置（低频）：三角色（创作/视觉识别/复核）连接与模型选择。
6. 项目保存/切换：保存当前工作区、从已保存项目继续。

## Product Priorities

按用户确认的"自己/团队高频使用"排序：

1. **efficiency** — 高频路径零冗余；从打开到开始制作的步骤与点击最少。
2. **clarity** — 当前状态、下一步动作、进行中任务一目了然。
3. **trust** — 进度、失败原因、重试语义透明；价格等关键信息准确呈现。
4. **low cognitive load** — 单层清晰的导航；高级能力（覆盖/四角/时段/原贴纸知识）不干扰主路径。
5. **visual quality** — 服务于以上四点；专业、克制、一致，不追求营销感。

## Existing UX Worth Preserving

- **五步流程骨架**（素材 → 包装 → 覆盖贴纸 → 导出 → 作品）：符合真实心智模型，保留为进度感知，但表现形式可重做。
- 作品列表的"统计行 + 任务列表"结构与信息密度。
- 包装页 subnav 将高级设置（显示时段/四角/覆盖/输出）与主模板选择分区的思路——分区合理，导航层级需简化。
- 深色侧边 rail + 浅色内容区的应用骨架。
- 空状态引导（如"第一条作品，从一份素材开始"）的引导意图，文案可更工具化。
- 进行中横幅（Agent 运行中/停止本轮）的可见性设计。

## Known Pain Points

来自真实渲染诊断（2026-09-22，截图存档 `/tmp/jianji-ui-inspect/`）与用户确认：

1. **字号系统性过小**：大量正文/说明/按钮文字 9–11px，可读性与专业感差（最大单一问题）。
2. **三层导航重叠**：rail + 头部五步 + 包装 subnav；"覆盖贴纸"两处重复；"导出"步骤名实为输出设置，语义混淆。
3. **营销腔与工具属性错配**：英文 eyebrow 装饰文案、CSS 手绘产品瓶插画、"等一份好素材"等文艺文案，对高频生产力用户是噪音。
4. **项目管理重复**：头部项目切换器与素材页项目卡片功能重复（保存/打开各出现两次）。
5. **贴纸库粗糙**：哈希名（"我的贴纸 0b7fb203"）无意义、每项重复红色删除链接、无视觉分组。
6. 用户总体反馈：视觉质量差、不够专业、整体设计不好看。

## Information Hierarchy

主闭环页面，按重要度：

1. **当前可否开始制作 / 开始动作**（主 CTA、缺什么阻止条件）。
2. 素材清单与选择状态（几条已选、是否就绪）。
3. 展示文字（价格）——合规关键，必须显眼。
4. 模板与条数。
5. 输出设置（目录/格式/分辨率/帧率/画质）。
6. 高级项：覆盖贴纸、四角贴纸、显示时段、原贴纸知识（默认不应抢占视觉）。
7. 环境状态（引擎/GPU/连接）——常态弱化，异常时凸显。

作品页：进行中任务与进度 > 本轮统计 > 已完成（播放/打开目录/追加）> 失败（原因/重试）。

## Design Freedom

用户明确授权"可以动结构"：

- 可合并/重组三层导航，重做页面内信息架构，只要主闭环（导入 → 设置 → 制作 → 作品）不被破坏。
- 可移除营销装饰（英文 eyebrow、手绘插画、文艺文案）。
- 可重做全部视觉系统：字体、字号阶梯、颜色、间距、组件语言、密度。
- 贴纸库、模型抽屉、连接页、覆盖/审阅流程的呈现均可重做。

## Hard Constraints

不可破坏（来自 CLAUDE.md 产品不变量与用户确认）：

- 展示文字必须用户手动输入，Agent 不得代填/改写；相关校验入口与文案语义保留。
- 保留原视频顺序、时长、音频；默认 720p 等比缩放补边、不裁剪。
- 本地渲染、先验证再发布、不覆盖源文件与已有输出。
- 冻结模板/重试语义：重试复用冻结方案，不重新调用模型。
- 核心制作闭环五步的操作顺序与准入规则（缺展示文字拒绝制作等）。
- 全部 `window.jianji` IPC 契约、shared schema、主进程行为——本次只改 renderer 呈现层。
- 桌面应用，无需移动端布局；但需覆盖约 760–1600px 窗口宽度。

## Design Direction

基于以上事实推导（推导者：设计执行方，非用户指定）：

- **定位：专业生产力工具**（类比 Linear / 剪映专业版），不是创意营销站。去除一切营销腔。
- **文字系统**：重建字号阶梯——正文/控件不低于 13px，辅助说明不低于 12px，彻底消灭 9–10px 文字；中文为主，装饰性英文 eyebrow 移除。
- **导航：单层化**。左侧 rail 承载主导航（制作/贴纸库/作品/模型），五步流程降级为页面内的轻量进度指示（非独立导航层）；包装高级设置保留分区但并入统一锚点导航。"导出"更名为"输出设置"类语义。
- **密度**：高频工具保持中高信息密度，拒绝大留白+大卡片风；卡片仅用于真实分组。
- **颜色**：保留深青绿为品牌基调（用户资产，贴纸/视频内容多为暖色促销风，界面应冷静衬托），收敛为有限 token 集；状态色（成功/失败/进行中/警告）语义化统一。
- **组件语言**：统一按钮/输入/标签/横幅/空状态/抽屉的单一实现与样式来源，消灭一处一样式。
- **装饰**：移除手绘 CSS 插画与场景图；模板卡片改用真实模板语义呈现（名称+特性），不再画产品瓶。

## Open Questions

- 模板卡片未来是否换真实效果缩略图（需本地生成样张，暂以语义化设计代替）。
- 作品页是否需要按项目/批次分组视图（当前按轮次，暂不阻塞）。
- Windows 高分屏（125%/150% 缩放）下的密度表现待实机验证。

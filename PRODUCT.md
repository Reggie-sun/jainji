# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

开发者本人及其小团队，每天高频批量出片的熟练用户（用户确认 2026-09-22）。电商运营视角，非专业视频编辑；关心出片数量、速度与稳定性。桌面 Windows / Linux 环境，窗口宽度约 760–1600px，无移动端场景。

## Product Purpose

简辑是本地桌面应用，为电商短视频做批量包装：导入产品原视频、手动填写展示文字（价格）、选择规则模板，Agent 为每条素材独立搭配四角贴纸与滤镜，本地 FFmpeg 批量导出独立 MP4。成功 = 一次设置，快速得到多条可直接发布、互不雷同的成品视频，过程可预期、失败可低成本重试。

## Positioning

把"每条视频的包装决策"委托给用户自己的模型连接（创作/视觉识别/复核三角色可独立配置），把渲染、合规与数据留在本地：新增文字仅限用户手动填写的展示文字，保留原视频顺序/时长/音频，本地 FFmpeg 先验证再发布，不覆盖源文件。邻居产品无法诚实复制的点：本地合规边界 + 模型自主搭配 + 批量冻结重试语义的组合。

## Operating Context

高频日用小闭环：导入素材 → 包装设置（模板/展示文字/条数/输出设置）→ 交给 Agent → 作品列表跟进导出 → 播放验收。辅助流程：追加制作（随机装饰零模型调用）、覆盖贴纸配置、贴纸库管理（本机已有 69 张上传促销贴纸）、项目保存/切换、低频的三角色模型配置。真实项目：氨糖膏、舒鼾膏、蝴蝶贴等健康产品。

## Capabilities and Constraints

- 展示文字必须用户手动输入（最多 2 行、每行 12 字），Agent 不得代填/改写；缺失时在调用模型前拒绝。
- 保留原视频顺序、时长、音频；默认按横竖方向导出 720p、原帧率，等比缩放补边，不裁剪。
- 本地渲染、先写临时输出验证再发布；不覆盖源文件与已有输出。
- 导出重试复用冻结方案与素材快照，不重新调用模型；重新生成包装重新创作。
- 覆盖贴纸为独立开关（默认关闭），自动/手动两种方式；原贴纸知识可跨项目复用。
- 一次制作最多 250 条，项目最多保留 100 条导出记录。
- 技术：Electron + React 18 + TypeScript + vite，手写 CSS（无 UI 框架），renderer 经 `window.jianji` IPC 与主进程通信；本次 redesign 只改 renderer 呈现层，不动 IPC 契约与主进程。

## Brand Commitments

- 名称：简辑 / JIANJI。
- 深青绿（teal）为既有品牌基调，是用户资产，redesign 保留其品牌角色（具体色值由 DESIGN.md 定义）。
- 用户绑定要求：去营销腔，专业生产力工具气质（2026-09-22 确认"你判断即可" + 高效优先）。

## Evidence on Hand

- 真实运行截图诊断存档：`/tmp/jianji-ui-inspect/`（2026-09-22，素材/包装/覆盖/输出/作品/贴纸库/模型抽屉/窄视口）。
- 产品与规则文档：`README.md`、`CLAUDE.md`、`docs/`（模型服务、覆盖流水线、源贴纸知识合同等）。
- 详细用户与任务分析：`PRODUCT_CONTEXT.md`（JTBD、优先级、信息层级、设计自由度）。
- 无 testimonials、无外部案例；未来工作不得编造。

## Product Principles

1. 效率优先：高频路径零冗余，从打开到开始制作的点击最少。
2. 状态透明：进度、失败原因、重试语义清晰可见。
3. 关键信息准确：价格等展示文字显眼、原样呈现。
4. 高级能力不干扰主路径：覆盖/四角/时段/原贴纸知识默认退后。
5. 专业克制：工具气质，拒绝营销装饰与视觉噪音。

## Accessibility & Inclusion

桌面键鼠交互；需可见焦点态、4.5:1 文字对比、`prefers-reduced-motion` 支持（现状已有，保留）。Windows 高分屏缩放下密度表现待实机验证。

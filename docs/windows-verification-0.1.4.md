# Windows 0.1.4 验证记录

日期：2026-09-23。Windows 11 x64（10.0.26200），Node.js 22.19.0。代码基线为 `origin/main` 的 `fce917d`，另含本分支沿用的 Windows 修复、桌面测试脚本适配及 0.1.4 版本号。所有模型、账号和反馈均使用隔离 fixture，没有调用真实商业模型或提交 GitHub Issue。

| 检查 | 结果 |
| --- | --- |
| 类型检查、前端与 Electron 构建、贴纸校验 | 通过；3,144 个 Fluent 与 70 个自定义贴纸文件有效 |
| 完整测试 | 123 个文件通过、2 个文件跳过；1,034 项通过、20 项跳过；使用内置 FFmpeg，受控并发 |
| 真实 Electron 桌面 smoke | PASS；隔离登录和取消、导入视频、本地随机导出 MP4；模型请求 0，原片未改，渲染异常 0 |
| 反馈 smoke | PASS；本地中继及 GitHub fixture；3 次请求，正常关闭后恢复回执，渲染异常 0 |
| 包内运行环境 | FFmpeg、ffprobe、Noto CJK 字体及许可证存在；在仅含系统目录的 PATH 下完成中文文字 H.264 编码及应用启动 |
| 源贴纸知识库正常退出 | 隔离运行时确认 `owner.lock` 在运行中存在，正常关闭后消失 |
| NSIS 构建 | 通过；x64，未配置代码签名或产品图标 |

桌面与反馈 smoke 最初因新版界面的入口文字、反馈页面标识改变而失败；修正测试脚本后均通过。该失败没有显示产品运行时异常。完整测试中的跳过项不计为验收通过；包括 Windows 当前权限不支持的符号链接用例、POSIX 故障注入、显式网络素材库测试及硬件相关用例。

最终安装包：`jianji-setup-0.1.4.exe`，469,740,350 字节。SHA-256：`988908fecb27eb987143fa88e107d501993749a3085dd63282d3e547568567a5`。本次未在有用户安装版运行的电脑上执行安装器的交互或静默安装、卸载，以免覆盖正在使用的安装目录；已验证打包后的 `win-unpacked` 内容与独立配置目录启动。目标 GPU、真实模型、长视频、成片人工观看仍按 [Windows 验收清单](windows-acceptance-spec.md) 执行。正常退出路径已验证会释放知识库锁，但不能推断此前用户异常退出的原因。
